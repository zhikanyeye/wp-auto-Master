name: Build and Release Plugin
run-name: "构建波克wpAI自动化插件 ${{ github.ref_name }}"

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build distributable plugin zip
        id: package
        run: |
          set -euo pipefail
          package_path="$(bash scripts/package-plugin.sh | sed -n 's/^Created //p' | tail -n 1)"
          package_name="$(basename "$package_path")"
          echo "path=$package_path" >> "$GITHUB_OUTPUT"
          echo "name=$package_name" >> "$GITHUB_OUTPUT"
          echo "version=${package_name#boke-wpai-automation-v}" | sed 's/\.zip$//' >> "$GITHUB_OUTPUT"

      - name: Validate distributable plugin zip
        run: |
          unzip -t "${{ steps.package.outputs.path }}"
          test "$(unzip -Z1 "${{ steps.package.outputs.path }}" | awk -F/ 'NF > 1 {print $1}' | sort -u)" = "bokeauto"
          ! unzip -Z1 "${{ steps.package.outputs.path }}" | grep -E '(^|/)(README\.md|\.github|\.monkeycode|scripts|tests?)(/|$)'

      - name: Publish build summary
        env:
          PACKAGE_NAME: ${{ steps.package.outputs.name }}
          PACKAGE_PATH: ${{ steps.package.outputs.path }}
        run: |
          {
            printf '# 波克wpAI自动化插件构建\n\n'
            printf '- 版本：`%s`\n' "${{ steps.package.outputs.version }}"
            printf '- 触发引用：`%s`\n' "${{ github.ref_name }}"
            printf '- 提交：`%s`\n' "${{ github.sha }}"
            printf '- 构建产物：`%s`\n' "$PACKAGE_NAME"
            printf '- 白名单校验：通过\n'
            printf '- 压缩包完整性校验：通过\n'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Generate release notes
        if: startsWith(github.ref, 'refs/tags/')
        id: release_metadata
        env:
          CURRENT_TAG: ${{ github.ref_name }}
          REPOSITORY: ${{ github.repository }}
          SERVER_URL: ${{ github.server_url }}
        run: |
          set -euo pipefail

          previous_tag=""
          found_current="false"
          while IFS= read -r tag; do
            if [ "$tag" = "$CURRENT_TAG" ]; then
              found_current="true"
              continue
            fi
            if [ "$found_current" = "true" ] && git merge-base --is-ancestor "$tag" "$CURRENT_TAG"; then
              previous_tag="$tag"
              break
            fi
          done < <(git tag --sort=-v:refname)

          if [ -n "$previous_tag" ]; then
            range="$previous_tag..$CURRENT_TAG"
            compare_url="$SERVER_URL/$REPOSITORY/compare/$previous_tag...$CURRENT_TAG"
          else
            range="$CURRENT_TAG"
            compare_url="$SERVER_URL/$REPOSITORY/commits/$CURRENT_TAG"
          fi

          {
            printf '# %s\n\n' "$CURRENT_TAG"
            printf '> 波克wpAI自动化插件 %s\n\n' "$CURRENT_TAG"
            printf '**构建产物：** `%s`\n\n' "${{ steps.package.outputs.name }}"
            printf '## 变更概览\n\n'
            if [ -n "$previous_tag" ]; then
              printf '本版本基于 `%s` 生成，完整变更记录：[查看对比]('%s')。\n\n' "$previous_tag" "$compare_url"
            else
              printf '这是首个标签版本，完整变更记录：[查看提交历史]('%s')。\n\n' "$compare_url"
            fi

            for section in 'feat|新增功能' 'fix|问题修复' 'perf|性能改进' 'refactor|代码重构' 'docs|文档变更' 'chore|工程维护' 'other|其他变更'; do
              type="${section%%|*}"
              title="${section#*|}"
              printf '## %s\n\n' "$title"
              case "$type" in
                feat) pattern='feat' ;;
                fix) pattern='fix' ;;
                perf) pattern='perf' ;;
                refactor) pattern='refactor' ;;
                docs) pattern='docs' ;;
                chore) pattern='chore|ci|build|test' ;;
                other) pattern='.' ;;
              esac

              count=0
              while IFS=$'\t' read -r subject short_hash; do
                [ -n "$subject" ] || continue
                type_prefix="${subject%%:*}"
                type_prefix="${type_prefix%!}"
                if [ "$type" = "other" ]; then
                  case "$type_prefix" in
                    feat|fix|perf|refactor|docs|chore|ci|build|test) continue ;;
                  esac
                elif ! printf '%s' "$type_prefix" | grep -Eq "^($pattern)$"; then
                  continue
                fi
                printf -- '- %s (`%s`)\n' "${subject#*: }" "$short_hash"
                count=$((count + 1))
              done < <(git log --format='%s%x09%h' "$range")

              if [ "$count" -eq 0 ]; then
                printf '暂无记录。\n'
              fi
              printf '\n'
            done
          } > release-notes.md

          if printf '%s' "$CURRENT_TAG" | grep -Eq -- '-(alpha|beta|rc)(\.|$)'; then
            echo 'prerelease=true' >> "$GITHUB_OUTPUT"
          else
            echo 'prerelease=false' >> "$GITHUB_OUTPUT"
          fi

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          name: 波克wpAI自动化插件 ${{ github.ref_name }}
          body_path: release-notes.md
          prerelease: ${{ steps.release_metadata.outputs.prerelease }}
          files: ${{ steps.package.outputs.path }}

      - name: Upload package artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.package.outputs.name }}
          path: ${{ steps.package.outputs.path }}
