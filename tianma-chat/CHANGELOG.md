# 更新日志 / Changelog

## [1.7.0] - 2026-08-13

### 核心改进
- **AI 回复改为后台异步执行，彻底消除整站卡顿**
  对话请求不再阻塞 Web 工作进程：POST 启动独立 PHP 进程执行智能体主循环，前端每 700ms 轮询增量拉取结果。修复了「AI 回复期间整站加载卡死」的问题。
- **修复任务工作中莫名中断**
  - 移除「连续 4 次工具失败强制中止」的熔断逻辑——工具调用失败不再中断任务，模型可自行重试或调整方案直至收敛；
  - 取消固定步数上限（默认 0 = 不限制，可在聊天页自定义）；
  - 前端轮询不再因「长时间无数据」误判超时中断，仅给出柔和提示并继续等待后台产出。
- **聊天页新增执行参数控件**
  可自定义「PHP 超时时间」与「最大工具步数」（均填 0 表示不限制），长任务更可控、更不容易被环境杀掉。

### 兼容性与稳定性
- **Linux / 生产环境适配**
  - 后台进程拉起改用 `nohup ... &`，彻底脱离 Web 请求所在会话，避免请求进程退出时被 SIGHUP 一并带走（否则异步会退化为同步、卡顿/超时回归）；
  - `find_php_cli()` 改为 OS 感知：Linux 用 `command -v`、Windows 用 `where`，并加绝对路径兜底（`/usr/bin/php`、`/usr/local/bin/php` 等），即使 `shell_exec` 被禁也能探测到 PHP CLI；
  - 路径安全校验兼容 Linux 绝对路径；`run-task.php` 纯跨平台（无 Windows 专用写法）。
- 修复一处因删除熔断代码块时多写一个 `}` 导致的 PHP 解析错误。

### 已知部署前提（生产环境）
- 主机需提供可用的 PHP CLI（通常在 `php` / `/usr/bin/php`），且未将 `exec` / `proc_open` 列入 `disable_functions`；否则自动降级为同步流式（仍有完整功能，但长任务会占用 Web 进程）。
- `wp-content/uploads` 目录需 Web 用户可写（异步流文件写在 `uploads/tianma-stream/`）。

---

## [1.6.0] - 2026-08-11
- 新增 Gitee 国内镜像链接（README / 设置页双仓库地址）。
- 新增演示 GIF（assets/tianma-demo.gif）。

## [1.5.9] - 2026-08-11
- 波克wpAI自动化插件核心能力补强与若干细节修复。

## [1.0.0] - 2026-08-09
- 首个正式版本。
- Agent 核心循环 + 40 项工具 + 向量记忆自学习 + 高危确认 + 审计日志。
